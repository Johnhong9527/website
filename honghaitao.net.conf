server
{
  # listen [::]:80;
  server_name honghaitao.net www.honghaitao.net;
  index index.html index.htm index.php default.html default.htm default.php;
  root  /home/wwwroot/ycoco.xyz;

  #include typecho.conf;
  error_page   404   /404.html;

  # Deny access to PHP files in specific directory
  location ~ /(wp-content|uploads|wp-includes|images)/.*\.php$ { deny all; }

  #include enable-php.conf;

  location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
  {
    expires      30d;
  }

  location ~ .*\.(js|css)?$
  {
    expires      12h;
  }

  location ~ /.well-known {
    allow all;
  }

  location ~ /\.
  {
    deny all;
  }

  access_log off;

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/www.honghaitao.net/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/www.honghaitao.net/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot



}
server {
  server_name blog.honghaitao.net;
  root /home/wwwroot/blog.ycoco.xyz;
  #include typecho.conf;
  error_page   404   /404.html;

  # Deny access to PHP files in specific directory
  location ~ /(wp-content|uploads|wp-includes|images)/.*\.php$ { deny all; }

  #include enable-php.conf;

  location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
  {
    expires      30d;
  }

  location ~ .*\.(js|css)?$
  {
    expires      12h;
  }

  location ~ /.well-known {
    allow all;
  }

  location ~ /\.
  {
    deny all;
  }

  access_log off;

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/blog.honghaitao.net/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/blog.honghaitao.net/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot


}
server
{
    if ($host = www.honghaitao.net) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    if ($host = honghaitao.net) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


  listen 80;
  server_name honghaitao.net www.honghaitao.net;
    return 404; # managed by Certbot




}
server {
    if ($host = blog.honghaitao.net) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


  listen 80;
  server_name blog.honghaitao.net;
    return 404; # managed by Certbot


}
